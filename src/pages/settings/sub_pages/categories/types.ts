// Form data interface for category forms
export interface CategoryFormData {
  name: string;
  description: string;
  color: string;
  icon: string;
  enabled: boolean;
  team_id: string;
  workspace_id: string;
  sla_id: string;
  approval_id: string;
  status_transition_group_id: string;
  celebration_effect: string;
}
